---
name: status
description: Usar para consultar el estado y la siguiente accion valida de un work item SDD.
---

Capability: `sdd.status`.
Usar `sdd-cli status`, `sdd-cli next --json` y `sdd-cli validate`; no inferir estado desde el chat.
